# Isolated local backup assessment

Backup: `origin/backup/cambios-locales-20260712` at `68bb2616906b84980317ae855a9aa1cc724679d5`. It remains isolated and was not merged or cherry-picked.

| File                                                            | Assessment                                                                                                                                                                                                                            | Decision                                                                                                                                                                                                          |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/shopify/abandoned-checkouts.ts`                            | Removes `customer { email }`; the admin abandoned-cart view then loses buyer identification. Shopify schema validation says the current query is valid but requires `read_orders` + `read_customers`; `Customer.email` is deprecated. | Do not merge as-is. PR7 should use a minimal `read_orders` query and optional enrichment via `defaultEmailAddress.emailAddress` only with intentional `read_customers`; preserve a recovery URL when implemented. |
| `supabase/aplicar-fix-lot-remaining.sql`                        | Manual SQL Editor/psql script duplicates the migration fix and directly inserts migration history.                                                                                                                                    | Do not deploy or commit as migration history; keep as recovery evidence pending disposable DB verification.                                                                                                       |
| `supabase/aplicar-migraciones-pendientes.sql`                   | 1,083-line bundle concatenates four versioned migrations and manually records versions. Normal migration use could double-apply changes or falsify history.                                                                           | Exclude from replacement PRs; replay individual ordered migrations only.                                                                                                                                          |
| `supabase/migrations/20260712120000_fix_lot_remaining_cast.sql` | Casts the `numeric` result of `sum(bigint)` window arithmetic to the declared `bigint`, addressing likely SQLSTATE `42804`; no later migration fixes it.                                                                              | Keep isolated; if reproduced locally, include exactly once after migration `20260711130000` in the database/loyalty PR.                                                                                           |

No backed-up file was discarded.
